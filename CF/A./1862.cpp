#include<bits/stdc++.h>
using namespace std;
int main () {
    ios_base::sync_with_stdio(0);cin.tie(0);
    int TC; cin >> TC;
    while (TC --) {
        int n,m;    cin >> n >> m;
        
    }
    return 0;
}